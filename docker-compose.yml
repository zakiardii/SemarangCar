version: '3.8'

services:
  semarangcar-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: semarangcar_api
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    restart: always
